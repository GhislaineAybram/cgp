export interface Video {
  id: string;
  date: Date;
  channel: string;
  title: string;
  text: string;
  link: string;
}

export interface VideoNew {
  date: Date;
  channel: string;
  title: string;
  text: string;
  link: string;
}
